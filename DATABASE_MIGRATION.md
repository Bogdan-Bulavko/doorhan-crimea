# 🗄️ Миграция базы данных на сервер

## 📋 Пошаговая инструкция

### 1. **Экспорт данных с локальной разработки**

```bash
# Экспорт в JSON формат (рекомендуется)
npm run export-database

# Или создание SQL дампа
npm run create-sql-dump
```

**Результат:**
- `database-export.json` - полный экспорт всех данных
- `database-dump.sql` - SQL дамп для прямого импорта

### 2. **Подготовка сервера**

#### 2.1. Установка зависимостей на сервере
```bash
# Клонирование репозитория
git clone https://github.com/your-repo/doorhan-crimea.git
cd doorhan-crimea

# Установка зависимостей
npm install

# Установка tsx для выполнения скриптов
npm install --save-dev tsx
```

#### 2.2. Настройка переменных окружения
```bash
# Создание .env файла на сервере
cp .env.example .env

# Редактирование .env для продакшена
nano .env
```

**Настройки для продакшена:**
```env
# База данных (PostgreSQL/MySQL)
DATABASE_URL="postgresql://user:password@localhost:5432/doorhan_crimea"
# или
DATABASE_URL="mysql://user:password@localhost:3306/doorhan_crimea"

# Окружение
NODE_ENV="production"

# Деплой
DEPLOY_TARGET="production"
```

### 3. **Перенос данных на сервер**

#### 3.1. Копирование файлов экспорта
```bash
# Копирование файлов на сервер
scp database-export.json user@server:/path/to/project/
scp database-dump.sql user@server:/path/to/project/
```

#### 3.2. Создание схемы базы данных
```bash
# На сервере
npm run prisma:generate
npm run prisma:push
```

#### 3.3. Импорт данных
```bash
# Импорт из JSON (рекомендуется)
npm run import-database

# Или импорт из SQL дампа
mysql -u user -p database_name < database-dump.sql
# или для PostgreSQL
psql -U user -d database_name -f database-dump.sql
```

### 4. **Проверка миграции**

```bash
# Проверка данных
npm run list-admins

# Открытие Prisma Studio для проверки
npm run prisma:studio
```

## 🔧 **Альтернативные методы**

### Метод 1: Прямой перенос SQLite файла
```bash
# Копирование SQLite файла
scp prisma/dev.db user@server:/path/to/project/prisma/

# Обновление DATABASE_URL в .env
DATABASE_URL="file:./prisma/dev.db"
```

### Метод 2: Использование Prisma Migrate
```bash
# Создание миграции
npx prisma migrate dev --name init

# Применение миграции на сервере
npx prisma migrate deploy
```

### Метод 3: Использование Prisma DB Push
```bash
# Принудительное применение схемы
npx prisma db push --force-reset
```

## 🚨 **Важные моменты**

### Безопасность
- ✅ Создайте резервную копию перед миграцией
- ✅ Проверьте права доступа к базе данных
- ✅ Используйте SSL соединения в продакшене

### Производительность
- ✅ Индексируйте часто используемые поля
- ✅ Оптимизируйте запросы
- ✅ Настройте connection pooling

### Мониторинг
- ✅ Настройте логирование
- ✅ Мониторинг производительности
- ✅ Регулярные бэкапы

## 📊 **Проверка целостности данных**

### После миграции выполните:
```bash
# Проверка количества записей
npm run list-admins

# Проверка связей
npx prisma studio

# Тестирование API
curl http://localhost:3000/api/products
```

## 🔄 **Откат изменений**

Если что-то пошло не так:
```bash
# Восстановление из бэкапа
cp database-backup.json database-export.json
npm run import-database

# Или восстановление схемы
npx prisma db push --force-reset
```

## 📞 **Поддержка**

При возникновении проблем:
1. Проверьте логи приложения
2. Убедитесь в правильности DATABASE_URL
3. Проверьте права доступа к базе данных
4. Обратитесь к документации Prisma

---

**Удачной миграции! 🚀**
