# Скрипты для управления пользователями

## 🚀 Быстрый старт

### Все доступные команды:
```bash
# Создание админа
npm run create-admin                    # Стандартный админ
npm run create-admin-custom             # Кастомный админ

# Управление админами
npm run list-admins                     # Список всех админов
npm run reset-admin-password            # Сброс пароля

# База данных
npm run prisma:studio                   # Prisma Studio
npm run prisma:push                     # Применить схему
npm run prisma:generate                 # Генерировать клиент
```

## 📋 Краткая справка

## Создание админа

### 1. Создание стандартного админа
```bash
npm run create-admin
```
Создает админа с данными:
- Email: `admin@doorhan-crimea.ru`
- Пароль: `admin123`
- Имя: `Администратор Системы`
- Роль: `admin`

### 2. Создание кастомного админа
```bash
npm run create-admin-custom
```
Или с параметрами:
```bash
npm run create-admin-custom "newadmin@example.com" "password123" "Имя" "Фамилия"
```

### 3. Прямое выполнение скрипта
```bash
npx tsx scripts/create-admin-custom.ts "admin@example.com" "password123" "Имя" "Фамилия"
```

## Параметры скрипта create-admin-custom

1. **Email** (обязательный) - email админа
2. **Пароль** (обязательный) - пароль админа
3. **Имя** (опциональный) - имя админа (по умолчанию: "Администратор")
4. **Фамилия** (опциональный) - фамилия админа (по умолчанию: "Системы")

## Примеры использования

```bash
# Создать админа с дефолтными данными
npm run create-admin-custom

# Создать админа с кастомным email и паролем
npm run create-admin-custom "superadmin@doorhan.ru" "superpassword123"

# Создать админа с полными данными
npm run create-admin-custom "manager@doorhan.ru" "manager123" "Менеджер" "Отдела"
```

## Безопасность

- Пароли автоматически хэшируются с помощью bcrypt
- Используется соль с 10 раундами
- Проверяется уникальность email
- Создаются только активные пользователи

## Сброс пароля админа

### Сброс пароля существующего админа
```bash
npm run reset-admin-password "admin@example.com" "newpassword123"
```

Параметры:
1. **Email** (обязательный) - email админа
2. **Новый пароль** (опциональный) - новый пароль (по умолчанию: "admin123")

### Примеры сброса пароля
```bash
# Сбросить пароль с дефолтным паролем
npm run reset-admin-password "admin@doorhan-crimea.ru"

# Сбросить пароль с кастомным паролем
npm run reset-admin-password "admin@doorhan-crimea.ru" "newpassword123"
```

## Просмотр всех админов

### Список всех админов
```bash
npm run list-admins
```

Показывает:
- ID админа
- Email
- Имя и фамилию
- Телефон
- Роль
- Статус активности
- Даты создания и обновления

## Проверка существующих админов

Для проверки существующих админов используйте Prisma Studio:
```bash
npm run prisma:studio
```

Или выполните SQL запрос в базе данных:
```sql
SELECT * FROM User WHERE role = 'admin';
```
