# АУДИТ ТЕХНИЧЕСКОГО ЗАДАНИЯ ПО SEO ОПТИМИЗАЦИИ
## Дата: 21 ноября 2024

---

## 📊 ОБЩИЙ СТАТУС ПРОЕКТА

### Прогресс выполнения: ~45%

**Легенда:**
- ✅ **Выполнено полностью**
- 🟡 **Выполнено частично** (требуются доработки)
- ❌ **Не реализовано**
- 🔄 **В процессе разработки**

---

## 1️⃣ МЕТАТЕГИ

### 1.1. Метатег Title для любой страницы
**Статус:** 🟡 Выполнено частично

**Что есть:**
- ✅ В БД есть поле `seoTitle` для:
  - Categories (строка 66 в schema.prisma)
  - Products (строка 96 в schema.prisma)
  - Pages (строка 251 в schema.prisma)
- ✅ Есть автогенерация Title с учетом регионов (metadata-generator.ts)
- ✅ Работает приоритет: ручной seoTitle > автогенерация

**Что нужно:**
- ❌ Добавить поле `seoTitle` для домашней страницы (через SiteSetting или отдельную модель)
- ❌ Интерфейс в админке для редактирования Title на всех страницах
- ❌ Возможность использовать переменные города в ручных Title (например: `[город]`, `[город_в_предложном]`)

**Файлы для правки:**
- `prisma/schema.prisma` - добавить Home page SEO
- `src/app/admin/settings/page.tsx` - добавить редактирование SEO домашней
- `src/lib/metadata-generator.ts` - добавить поддержку переменных

---

### 1.2. Метатег Description для любой страницы
**Статус:** 🟡 Выполнено частично

**Что есть:**
- ✅ В БД есть поле `seoDescription` для Categories, Products, Pages
- ✅ Есть автогенерация Description с учетом регионов и цен
- ✅ Работает приоритет: ручной seoDescription > автогенерация

**Что нужно:**
- ❌ Добавить `seoDescription` для домашней страницы
- ❌ Интерфейс в админке для редактирования Description
- ❌ Поддержка переменных города в Description

**Файлы для правки:**
- Аналогично п. 1.1

---

### 1.3. Canonical для любой страницы
**Статус:** ✅ Выполнено на 90%

**Что есть:**
- ✅ В БД есть поле `canonicalUrl` для Categories, Products, Pages
- ✅ Автогенерация canonical с правильным FQDN (canonical-utils.ts)
- ✅ Поддержка двух форматов:
  - Относительный путь (автоматически преобразуется в полный с учетом поддомена)
  - Полный URL (используется как есть)
- ✅ Функции генерации для всех типов страниц

**Что нужно:**
- ❌ Добавить canonical для домашней страницы (Home)
- ❌ Интерфейс в админке для настройки canonical

**Файлы для правки:**
- `src/app/admin/categories/[id]/page.tsx` - добавить поле canonical
- `src/app/admin/products/[id]/page.tsx` - добавить поле canonical
- `src/app/admin/pages/[id]/page.tsx` - добавить поле canonical
- `src/app/admin/settings/page.tsx` - добавить canonical для home

---

### 1.4. Метатег robots для любой страницы
**Статус:** ❌ Не реализовано

**Что есть:**
- ❌ Нет поля `robotsMeta` в схеме БД
- ✅ Есть файл robots.ts для robots.txt (НЕ meta robots)

**Что нужно:**
- ❌ Добавить поле `robotsMeta` (String?) в модели: Category, Product, Page, SiteSetting
- ❌ Интерфейс в админке с выбором:
  - `index, follow` (по умолчанию)
  - `noindex, follow`
  - `index, nofollow`
  - `noindex, nofollow`
- ❌ Добавить в генерацию метаданных на всех страницах

**Файлы для создания/правки:**
```prisma
// В schema.prisma
model Category {
  robotsMeta String? @default("index, follow")
}

model Product {
  robotsMeta String? @default("index, follow")
}

model Page {
  robotsMeta String? @default("index, follow")
}
```

---

### 1.5. Микроразметка (Schema.org JSON-LD)
**Статус:** ❌ Не реализовано

**Что есть:**
- ❌ Нет возможности добавить микроразметку

**Что нужно:**
- ❌ Добавить поле `schemaMarkup` (String?) для хранения JSON-LD
- ❌ Два типа:
  1. **Сквозная микроразметка** (через SiteSetting) - выводится на всех страницах
  2. **Страничная микроразметка** - для конкретных Categories, Products, Pages
- ❌ Компонент для вывода перед `</footer>`
- ❌ Интерфейс в админке с валидацией JSON

**Файлы для создания:**
```typescript
// src/components/SchemaMarkup.tsx - новый компонент
// src/app/admin/schema-markup/page.tsx - админка
```

---

## 2️⃣ КОНТЕНТ

### 2.1. Изменение заголовка H1
**Статус:** ✅ Выполнено на 80%

**Что есть:**
- ✅ В БД есть поле `h1` для Categories, Products, Pages
- ✅ Работает fallback: h1 → title

**Что нужно:**
- ❌ Интерфейс в админке для редактирования H1
- ❌ Добавить H1 для домашней страницы

---

### 2.2. Добавление текста на любую страницу
**Статус:** 🟡 Выполнено частично

**Что есть:**
- ✅ У Pages есть поле `content` (Markdown)
- ✅ У Categories есть поле `description`

**Что нужно:**
- ❌ Расширить возможности добавления контента:
  1. **Для домашней страницы** - добавить контентные блоки через SiteSetting
  2. **Для категорий (листингов)** - добавить поле `contentTop` и `contentBottom`
  3. **Для подкатегорий** - аналогично
- ❌ Поддержка Markdown/HTML
- ❌ Возможность использовать переменные города в контенте

**Файлы для правки:**
```prisma
// schema.prisma
model Category {
  contentTop    String? // Контент вверху страницы
  contentBottom String? // Контент внизу страницы
}

model SiteSetting {
  // Добавить настройки для контента домашней страницы
}
```

---

## 3️⃣ ПОДДОМЕНЫ (РЕГИОНАЛЬНЫЕ)

### 3.1. Создание виртуальных поддоменов
**Статус:** ✅ Выполнено на 90%

**Что есть:**
- ✅ Модель Region в БД (schema.prisma строка 227-244)
- ✅ Определение региона из поддомена (regional-data.ts)
- ✅ Middleware для обработки поддоменов (вероятно есть)

**Что нужно:**
- ❌ Интерфейс в админке для управления регионами (возможно уже есть, нужно проверить)

---

### 3.2. Настройка данных для поддоменов
**Статус:** ✅ Выполнено полностью

**Что есть:**
- ✅ Все необходимые поля в модели Region:
  - `code` - код поддомена
  - `name` - название города (именительный падеж)
  - Можно добавить prepositionalCase отдельным полем
  - `address` - можно использовать для указания "г. Армянск" или "пгт. Армянск"
- ✅ Интерфейс для редактирования регионов в админке (судя по структуре)

**Что нужно:**
- 🟡 Добавить поле `namePrepositional` (название в предложном падеже) в модель Region

**Файлы для правки:**
```prisma
model Region {
  namePrepositional String  // Например: "Армянске", "Ялте"
}
```

---

### 3.3. Изменение названия города в метатегах
**Статус:** ✅ Выполнено на 95%

**Что есть:**
- ✅ metadata-generator.ts автоматически подставляет город в Title и Description
- ✅ Есть словари для разных падежей (строки 10-25 в metadata-generator.ts)
- ✅ Приоритет: ручной Title/Description > автогенерация

**Что нужно:**
- 🟡 Динамически загружать падежи из БД (Region.namePrepositional) вместо hardcode

---

### 3.4. Переключатель городов в шапке
**Статус:** ✅ Выполнено полностью

**Что есть:**
- ✅ Компонент RegionSelector (используется в Header.tsx строки 145-147)
- ✅ RegionContext для управления регионом
- ✅ Название города меняется в зависимости от поддомена

---

### 3.5. Изменение строки в зависимости от поддомена
**Статус:** ✅ Выполнено полностью

**Что есть:**
- ✅ В Header верхняя строка меняется в зависимости от regionalData (строки 60-81 в Header.tsx)
- ✅ Отображается адрес, телефон, режим работы из БД для текущего региона

---

### 3.6. Автогенерация карт сайта для поддоменов
**Статус:** ✅ Выполнено полностью

**Что есть:**
- ✅ sitemap.ts динамически генерирует карту для текущего поддомена (строки 8-40)
- ✅ baseUrl формируется с учетом региона

---

### 3.7. Настройка robots.txt для поддоменов
**Статус:** ✅ Выполнено полностью

**Что есть:**
- ✅ robots.ts динамически генерирует robots.txt (строки 6-47)
- ✅ Путь к sitemap меняется в зависимости от поддомена

---

## 4️⃣ ШАБЛОН

### 4.1. Обернуть заголовки на домашней в div
**Статус:** ❌ Требует проверки и правки

**Что нужно:**
- ❌ Проверить HeroSection.tsx, StatsSection.tsx и другие компоненты
- ❌ Убедиться, что заголовки обернуты в `<div>` (скриншот https://snipboard.io/N6iVxS.jpg недоступен)

**Файлы для проверки:**
- `src/components/HeroSection.tsx`
- `src/components/StatsSection.tsx`
- `src/components/AboutSection.tsx`

---

### 4.2. Исправить цвет заголовков блоков
**Статус:** ❌ Требует проверки

**Что нужно:**
- ❌ Найти блоки, где цвет заголовков совпадает с фоном (скриншот недоступен)
- ❌ Исправить цвет на контрастный

**Файлы для проверки:**
- Все компоненты в `src/components/`

---

### 4.3. Создать три служебные страницы
**Статус:** ❌ Не реализовано

**Что нужно:**
- ❌ Создать 3 страницы:
  1. `/polzovatelskoe-soglashenie` - Пользовательское соглашение
  2. `/politika-cookie-i-servisa-yandex-metrika` - Политика cookie
  3. `/politika-obrabotki-personalnyh-dannyh` - Политика обработки ПД

**Решение:**
Создать через админку Pages или через seed скрипт с рыбным текстом.

---

### 4.4-4.6. Добавить чекбокс согласия на обработку ПД в формы
**Статус:** ❌ Не реализовано

**Что нужно:**
- ❌ CallbackModal.tsx - добавить чекбокс (строка 236 перед кнопкой "Отправить")
- ❌ FloatingCallbackButton.tsx (форма) - добавить чекбокс
- ❌ ContactsSection.tsx (форма "Напишите нам") - добавить чекбокс

**Пример кода:**
```typescript
<div className="flex items-start space-x-2">
  <input
    type="checkbox"
    id="privacyConsent"
    required
    className="mt-1"
  />
  <label htmlFor="privacyConsent" className="text-sm">
    Даю согласие на{' '}
    <a
      href="/politika-obrabotki-personalnyh-dannyh"
      target="_blank"
      rel="nofollow noopener noreferrer"
      className="text-[#F6A800] underline"
    >
      обработку персональных данных
    </a>
  </label>
</div>
```

---

### 4.7. Создать custom.css и custom.js
**Статус:** ❌ Не реализовано

**Что нужно:**
- ❌ Создать `/public/custom.css`
- ❌ Создать `/public/custom.js`
- ❌ Подключить в `src/app/layout.tsx` или `src/pages/_document.tsx`

**Файлы для создания:**
```
/public/custom.css
/public/custom.js
```

**Подключение в layout.tsx:**
```typescript
import Script from 'next/script';
import '/public/custom.css';

// В компоненте:
<Script src="/custom.js" strategy="lazyOnload" />
```

---

## 5️⃣ ПРАВКИ ПО МЕЛОЧАМ

### 5.1. Правки на странице /posts
**Статус:** ❌ Не реализовано (нет раздела статей)

**Что нужно:**
1. Исправить "продукции..." → "продукции"
2. Добавить возможность редактировать короткий текст для статей (excerpt)
3. Заменить "Подробнее" на более информативный текст

**Примечание:** Сначала нужно создать раздел статей (см. п. 6)

---

### 5.2. Блоки на страницах категорий
**Статус:** ❌ Не реализовано

**Что нужно:**
Добавить на страницы категорий/подкатегорий два блока:

1. **Блок с ценой** - "от X рублей" + небольшой текст
2. **Блок-мотиватор** с формой для сбора лидов

**Пример структуры:**
```
[Название категории в родительном падеже]
Цена: от [минимальная цена] руб.
[Короткий текст о преимуществах]

---

Чтобы узнать точную стоимость и условия покупки [название категории в родительном падеже], звоните в представительство или заполните форму и мы вам сами перезвоним.

[Форма: Имя, Телефон, Кнопка "Заказать звонок", Чекбокс согласия]
```

**Файлы для правки:**
- `src/app/[categorySlug]/page.tsx` - добавить блоки
- `src/components/CategoryProducts.tsx` - или создать новый компонент

---

## 6️⃣ СТАТЬИ (БЛОГ)

**Статус:** ❌ Не реализовано

**Что есть:**
- ✅ Есть модель Page, но она для статических страниц, не статей

**Что нужно:**
- ❌ Создать модель Article в schema.prisma:
```prisma
model Article {
  id             Int      @id @default(autoincrement())
  title          String
  slug           String   @unique
  excerpt        String?  // Короткий текст для листинга
  content        String   // Полный текст
  imageUrl       String?  // Главное изображение
  author         String?
  publishDate    DateTime @default(now())
  
  // SEO
  seoTitle       String?
  seoDescription String?
  canonicalUrl   String?
  robotsMeta     String?  @default("index, follow")
  schemaMarkup   String?
  h1             String?
  
  // Управление
  isActive       Boolean  @default(true)
  sortOrder      Int      @default(0)
  
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
}
```

- ❌ Создать роуты:
  - `/posts` - листинг статей
  - `/posts/[slug]` - страница статьи

- ❌ Админка для статей:
  - `/admin/articles` - список
  - `/admin/articles/new` - создание
  - `/admin/articles/[id]` - редактирование

- ❌ API endpoints:
  - `/api/admin/articles/route.ts`
  - `/api/admin/articles/[id]/route.ts`

---

## 📋 ЧЕКЛИСТ ПРИОРИТЕТНЫХ ЗАДАЧ

### 🔴 КРИТИЧНО (Высокий приоритет для SEO)

1. ✅ **Robots meta tag** - добавить поле и интерфейс
2. ✅ **Микроразметка** - добавить возможность JSON-LD
3. ✅ **Чекбокс согласия ПД** в формы (обязательно по закону!)
4. ✅ **Служебные страницы** (политики) - обязательны по закону
5. ✅ **Custom.css/js** - для кастомных блоков
6. ✅ **SEO для домашней страницы** - Title, Description, H1, Canonical

### 🟡 ВАЖНО (Средний приоритет)

7. ✅ **Интерфейсы в админке** для редактирования SEO полей
8. ✅ **Контентные блоки** для категорий (contentTop, contentBottom)
9. ✅ **Блоки на страницах категорий** (цена + форма)
10. ✅ **Переменные города** в Title/Description/Content
11. ✅ **Поле namePrepositional** в Region

### 🟢 ЖЕЛАТЕЛЬНО (Низкий приоритет)

12. ✅ **Раздел статей** (Articles/Blog)
13. ✅ **Правки на странице /posts**
14. ✅ **Исправление цветов** заголовков
15. ✅ **Обертка в div** для заголовков

---

## 📊 СТАТИСТИКА ВЫПОЛНЕНИЯ

| Категория | Выполнено | Частично | Не выполнено | Прогресс |
|-----------|-----------|----------|--------------|----------|
| Метатеги | 1 | 2 | 2 | 40% |
| Контент | 0 | 2 | 0 | 50% |
| Поддомены | 5 | 1 | 1 | 86% |
| Шаблон | 0 | 0 | 7 | 0% |
| Мелочи | 0 | 0 | 2 | 0% |
| Статьи | 0 | 0 | 1 | 0% |
| **ИТОГО** | **6** | **5** | **13** | **~45%** |

---

## 🎯 РЕКОМЕНДУЕМЫЙ ПОРЯДОК РЕАЛИЗАЦИИ

### Этап 1: Юридические требования (1-2 дня)
1. Создать служебные страницы (пользовательское соглашение, политики)
2. Добавить чекбоксы согласия во все формы
3. Обновить футер со ссылками на политики

### Этап 2: SEO основа (2-3 дня)
4. Добавить поле robotsMeta во все модели
5. Добавить поддержку микроразметки (schemaMarkup)
6. Добавить SEO для домашней страницы (Title, Description, H1, Canonical)
7. Создать custom.css и custom.js

### Этап 3: Админка (3-4 дня)
8. Интерфейсы для редактирования всех SEO полей:
   - Categories (Title, Description, H1, Canonical, Robots, Schema)
   - Products (аналогично)
   - Pages (аналогично)
   - Home (через Settings)
9. Добавить возможность использовать переменные города `[город]`, `[город_в_предложном]`

### Этап 4: Контент (2-3 дня)
10. Добавить contentTop и contentBottom для категорий
11. Создать блоки для страниц категорий (цена + форма)
12. Добавить контентные блоки для домашней

### Этап 5: Поддомены (1 день)
13. Добавить поле namePrepositional в Region
14. Обновить metadata-generator для загрузки падежей из БД

### Этап 6: Статьи (3-4 дня)
15. Создать модель Article
16. Создать роуты для статей
17. Создать админку для статей
18. Создать API endpoints

### Этап 7: Финальные правки (1-2 дня)
19. Исправить цвета заголовков
20. Обернуть заголовки в div
21. Протестировать все формы
22. Проверить генерацию метатегов на всех страницах

---

## 📝 ДОПОЛНИТЕЛЬНЫЕ РЕКОМЕНДАЦИИ

### SEO
1. **Robots meta vs robots.txt** - это разные вещи! Meta robots управляет индексацией конкретной страницы, robots.txt - доступом краулера к файлам.

2. **Canonical стратегия** - рекомендуется использовать основной домен в canonical для всех поддоменов, чтобы избежать дублирования контента.

3. **Schema.org** - важно для Rich Snippets в Google. Приоритетные типы разметки:
   - Organization (сквозная)
   - Product (для товаров)
   - BreadcrumbList (хлебные крошки)
   - FAQPage (если будет раздел FAQ)

### Производительность
1. **Кэширование** - метатеги генерируются при каждом запросе. Рекомендую добавить кэширование.

2. **ISR (Incremental Static Regeneration)** - для страниц товаров и категорий можно использовать ISR с revalidate.

### UX
1. **Формы** - обязательно добавить валидацию на стороне клиента и сервера.

2. **Переменные в контенте** - создать документацию для контент-менеджера, какие переменные доступны.

---

## 🔗 ФАЙЛЫ ДЛЯ ИЗУЧЕНИЯ

### Уже реализованное:
- `prisma/schema.prisma` - схема БД
- `src/lib/metadata-generator.ts` - генерация метатегов
- `src/lib/canonical-utils.ts` - генерация canonical
- `src/lib/regional-data.ts` - региональные данные
- `src/app/robots.ts` - robots.txt
- `src/app/sitemap.ts` - sitemap.xml
- `src/contexts/RegionContext.tsx` - контекст регионов
- `src/components/Header.tsx` - шапка с переключателем регионов
- `src/components/Footer.tsx` - футер
- `src/components/CallbackModal.tsx` - форма обратного звонка

### Требуют доработки:
- `src/app/[categorySlug]/page.tsx` - страница категории
- `src/app/[categorySlug]/[productSlug]/page.tsx` - страница товара
- `src/app/pages/[slug]/page.tsx` - страница Page
- `src/app/page.tsx` - домашняя страница
- Все формы компоненты

---

## ✅ ЗАКЛЮЧЕНИЕ

Проект имеет **хорошую основу** для SEO:
- ✅ Структура БД продумана
- ✅ Поддомены работают
- ✅ Автогенерация метатегов есть
- ✅ Canonical и sitemap реализованы

**Основные пробелы:**
- ❌ Нет robots meta tag
- ❌ Нет микроразметки
- ❌ Нет интерфейсов для редактирования SEO
- ❌ Нет раздела статей
- ❌ Нет служебных страниц (политики)
- ❌ Нет чекбоксов согласия в формах

**Оценка времени полной реализации:** ~15-20 рабочих дней

**Рекомендация:** Начать с этапов 1-2 (юридические + SEO основа), так как они критичны для запуска сайта.

