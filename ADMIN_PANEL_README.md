# 🚀 DoorHan Crimea Admin Panel

## 📋 Описание

Полнофункциональная админ-панель для управления сайтом DoorHan Crimea с продвинутой системой загрузки и оптимизации производительности.

## ✨ Основные возможности

### 🎛️ Административные функции
- **Dashboard** - главная панель с статистикой
- **Управление товарами** - CRUD операции с товарами
- **Управление категориями** - создание и редактирование категорий
- **Управление заказами** - обработка заказов и статусов
- **Управление пользователями** - администрирование пользователей с ролями
- **Статистика** - аналитика и отчеты
- **Формы** - обработка заявок и обращений

### 🚀 Система производительности
- **Skeleton Loading** - скелетоны загрузки для всех страниц
- **Progress Bars** - индикаторы прогресса навигации
- **Кеширование API** - оптимизация запросов с TTL
- **Параллельная загрузка** - одновременная загрузка данных
- **Оптимистичные обновления** - мгновенная обратная связь
- **Прелоадеры** - предзагрузка соседних маршрутов

### 🔧 Технические особенности
- **TypeScript** - строгая типизация
- **Next.js 14** - App Router с Server Components
- **Prisma** - ORM для работы с базой данных
- **NextAuth.js** - аутентификация и авторизация
- **Tailwind CSS** - современный дизайн
- **ESLint** - без ошибок линтера

## 🛠️ Установка и настройка

### 1. Клонирование проекта
```bash
git clone https://github.com/Bogdan-Bulavko/doorhan-crimea.git
cd doorhan-crimea
```

### 2. Восстановление ветки с админкой
```bash
# Если у вас есть bundle файл
git bundle unbundle doorhan-admin-panel.bundle
git checkout feature/admin-panel

# Или создание новой ветки из bundle
git checkout -b feature/admin-panel
git pull ../doorhan-admin-panel.bundle feature/admin-panel
```

### 3. Установка зависимостей
```bash
npm install
```

### 4. Настройка базы данных
```bash
# Создание базы данных
npx prisma generate
npx prisma db push

# Заполнение тестовыми данными (опционально)
npx prisma db seed
```

### 5. Настройка переменных окружения
Создайте файл `.env.local`:
```env
DATABASE_URL="file:./dev.db"
NEXTAUTH_SECRET="your-secret-key"
NEXTAUTH_URL="http://localhost:3000"
```

### 6. Запуск проекта
```bash
npm run dev
```

## 📁 Структура проекта

```
src/
├── app/
│   ├── admin/                 # Админ-панель
│   │   ├── _components/       # Переиспользуемые компоненты
│   │   ├── categories/        # Управление категориями
│   │   ├── products/          # Управление товарами
│   │   ├── orders/            # Управление заказами
│   │   ├── users/             # Управление пользователями
│   │   ├── stats/             # Статистика
│   │   └── forms/             # Обработка форм
│   ├── api/                   # API endpoints
│   │   ├── admin/             # Админские API
│   │   └── auth/              # Аутентификация
│   └── signin/                 # Страница входа
├── lib/                       # Утилиты и конфигурация
│   ├── auth.ts               # Настройки аутентификации
│   ├── db.ts                 # Подключение к БД
│   ├── cache.ts              # Система кеширования
│   └── optimistic-updates.ts # Оптимистичные обновления
└── components/               # Общие компоненты
```

## 🎨 Компоненты системы загрузки

### SkeletonLoader
- `TableSkeleton` - для таблиц
- `CardSkeleton` - для карточек
- `GridSkeleton` - для сеток
- `StatsSkeleton` - для статистики
- `FormSkeleton` - для форм

### ProgressBar
- Глобальный прогресс-бар навигации
- Спиннеры разных размеров
- Кнопки с состоянием загрузки

### LoadingStates
- `LoadingState` - общее состояние загрузки
- `ErrorState` - состояние ошибки
- `EmptyState` - пустое состояние
- `LoadingOverlay` - оверлей для форм

## 🔐 Аутентификация

Система использует NextAuth.js с поддержкой:
- Email/Password аутентификация
- Роли пользователей (admin, manager, customer)
- Защищенные маршруты
- Автоматическое перенаправление

## 📊 База данных

Схема включает:
- **Users** - пользователи с ролями
- **Products** - товары с изображениями
- **Categories** - категории товаров
- **Orders** - заказы с статусами
- **Forms** - заявки и обращения
- **AdminLogs** - логи действий

## 🚀 Развертывание

### Vercel (рекомендуется)
```bash
npm install -g vercel
vercel --prod
```

### Docker
```bash
docker build -t doorhan-admin .
docker run -p 3000:3000 doorhan-admin
```

## 📝 API Endpoints

### Админские API
- `GET /api/admin/users` - список пользователей
- `POST /api/admin/users` - создание пользователя
- `GET /api/admin/products` - список товаров
- `POST /api/admin/products` - создание товара
- `GET /api/admin/orders` - список заказов
- `GET /api/admin/stats` - статистика

### Аутентификация
- `POST /api/auth/signin` - вход
- `POST /api/auth/signout` - выход
- `GET /api/auth/session` - текущая сессия

## 🎯 Особенности производительности

1. **Кеширование** - API ответы кешируются на 2-5 минут
2. **Suspense** - компоненты загружаются асинхронно
3. **Оптимистичные обновления** - UI обновляется мгновенно
4. **Параллельная загрузка** - множественные запросы выполняются одновременно
5. **Прелоадеры** - соседние страницы загружаются заранее

## 🔧 Разработка

### Запуск в режиме разработки
```bash
npm run dev
```

### Проверка типов
```bash
npm run type-check
```

### Линтинг
```bash
npm run lint
```

### Сборка
```bash
npm run build
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте настройки базы данных
2. Убедитесь в правильности переменных окружения
3. Проверьте логи в консоли браузера
4. Убедитесь, что все зависимости установлены

## 🎉 Готово к использованию!

Админ-панель полностью готова к работе и включает все необходимые функции для управления сайтом DoorHan Crimea.
